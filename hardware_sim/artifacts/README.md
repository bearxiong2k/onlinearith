# Generated hardware artifacts

All local build products go under this directory: simulator outputs, VCD/FST
waves, generated ledgers, netlists, synthesis logs/reports, characterization
tables, and plots.

They are ignored by default. If the user explicitly requests a durable paper
evidence snapshot, commit a small manifest/config/report set with source and
tool checksums rather than an opaque build tree.

