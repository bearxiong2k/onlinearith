# Anchor candidate origin

All SystemVerilog, include, testbench, and Yosys files below this directory
were copied byte-for-byte on 2026-08-01 from `../anchors` revision:

```text
5959bb8370605710abc8200d404cc1dfee682aff
```

Selection is limited to conditionally reusable metadata/window control,
associated smoke/synthesis templates, and generic metadata storage. It
explicitly excludes Anchor 2, Anchor 3, completion/payload storage, the old
window-config store, and the old integrated storage top.

These files are reference candidates only. Do not include this directory in
active builds.

Run `sha256sum -c SHA256SUMS` from this directory to verify the extracted
candidate set independently of the sibling repository.
